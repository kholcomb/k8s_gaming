💡 Near-Solution Hint:

Steps to fix:
1. Delete from default namespace: `kubectl delete -f broken.yaml`
2. Edit broken.yaml and change ALL `namespace: default` to `namespace: k8squest`
3. Apply to correct namespace: `kubectl apply -f broken.yaml`
4. Verify: `kubectl get all -n k8squest`

All resources should now be in the k8squest namespace!
